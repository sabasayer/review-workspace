# Run locally and offline

Version one runs as a loopback-only local server that opens one Review Bundle in a browser and performs no AI, forge, telemetry, update-check, or implicit network requests. This permits filesystem watching and atomic writes without API credentials while keeping source code and review data on the Reviewer's machine.
